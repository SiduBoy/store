<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jyb.store.mapper.UserMapper">

    <!--
        resultMap：当表的资源和累的最想属性字段名称不一致时,来自定义查询结果集映射规则
        id：给这个分配一个唯一的ID值，对应的是下面查询语句的“resultMap=”
        type:取值是一个类，表示的是数据库中的查询结果集与java 中哪个实体类进行结果集映射
        将表的资源和类的属性不一致的字段进行匹配，字段一致的可以省略
    -->
    <sql id="Base_Column_List">
        `uid`,
        is_delete,
        created_user,
        created_time,
        modified_user,
        modified_time
    </sql>
    <resultMap id="UserEntityMap" type="com.jyb.store.entity.User">
        <!--定义映射规则中主键是不可以省略的-->
        <id column="uid" property="uid"/>
        <result column="is_delete" property="isDelete"/>
        <result column="created_user" property="createdUser"/>
        <result column="created_time" property="createdTime"/>
        <result column="modified_user" property="modifiedUser"/>
        <result column="modified_time" property="modifiedTime"/>
    </resultMap>

    <!--
        id属性：表示映射接口中方法的名称
        useGeneratedKEys属性：表示开启某个字段的值递增
        keyProperty属性：开启的字段
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">
        insert into
            t_user(username,password,salt,phone,email,gender,avatar,is_delete,created_user,created_time,modified_user,modified_time)
        values
            (#{username},#{password},#{salt},#{phone}, #{email},#{gender},#{avatar},#{isDelete},#{createdUser},#{createdTime},#{modifiedUser},#{modifiedTime})
    </insert>

    <!--
        resultType:表示查询的结果集类型，只需要指定对应映射类的类型，并且包完整包接口
    -->
    <select id="findUserName" resultMap="UserEntityMap" resultType="com.jyb.store.mapper.UserMapper">
        select * from t_user
        where  username = #{username}
    </select>


<!--auto generated by MybatisCodeHelper on 2022-07-28-->
    <update id="updatePasswordByUid">
        update t_user set
        password=#{password},
        modified_user=#{modifiedUser},
        modified_time=#{modifiedTime}
        where uid=#{uid}
    </update>

    <select id="findByUid" resultMap="UserEntityMap">
        select * from t_user
        where uid=#{uid}
    </select>
</mapper>